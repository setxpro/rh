import styled from 'styled-components';

export const Button = styled.button`
    padding: .5rem 1.5rem;
    border: none;
    border-radius: 6px;
    background: #059;
    color: #FFFF;
    font-weight: bold;
    font-size: 1rem;

    cursor: pointer;
    transition: .5s;

    &:hover {
        opacity: 0.82;
    }
`;
